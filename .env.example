# SA password for the Dockerized SQL Server container (docker compose only —
# local dev uses Windows Integrated Security and needs no password here).
# Must satisfy SQL Server complexity rules: 8+ characters, upper+lower+digit+symbol.
MSSQL_SA_PASSWORD=Your_password123!
